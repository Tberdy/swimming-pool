ngDescribe({
    name: 'Test FriendsQueryService',
    modules: 'app',
    inject: 'FriendsQueryService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
