ngDescribe({
    name: 'Test ContentQueryService',
    modules: 'app',
    inject: 'ContentQueryService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
