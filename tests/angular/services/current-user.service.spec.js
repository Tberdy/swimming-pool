ngDescribe({
    name: 'Test CurrentUserService',
    modules: 'app',
    inject: 'CurrentUserService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
